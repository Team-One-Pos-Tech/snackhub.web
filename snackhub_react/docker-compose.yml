version: '3.4'

services:
  snackhubweb:
    image: snackhub.web
    container_name: snackhub.web
    build:
      context: .
      dockerfile: ./src/Dockerfile
    ports:
      - "42046:80"
    networks:
      - backend
networks:
  backend:
    driver: bridge
